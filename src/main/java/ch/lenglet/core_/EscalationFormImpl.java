package ch.lenglet.core_;

class EscalationFormImpl extends AbstractForm implements Form.EscalationForm {

    public EscalationFormImpl(
            FormData formData
    ) {
        super(formData);
    }
}
